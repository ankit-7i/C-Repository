#include<stdio.h>
int a =10;
main(){
    int a=20;
    printf("a=%d\n",a);

}
// Note When local and global variables have the same name, the local variable is used in the function. The global variable is not.